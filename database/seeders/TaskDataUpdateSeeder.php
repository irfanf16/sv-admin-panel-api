<?php

namespace Database\Seeders;

// use App\Models\TaskDataUpdateModel;
use http\Env\Request;
use Illuminate\Database\Seeder;

class TaskDataUpdateSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // set_time_limit(0);
        // $activities = (new TaskDataUpdateModel())->getAllActivities();

        // foreach ($activities as $activity){
        //     $req_array = (new TaskDataUpdateModel())->getEditedTaskData($activity);
        //     $result = (new TaskDataUpdateModel())->updateTaskData(null,$req_array, $activity);
        //     if($result)
        //         dump('Activity ID: '. $activity->activity_id.' Activity Title: '. $activity->title.' edited.');
        // }

    }

}
